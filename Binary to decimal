#include<iostream>
using namespace std;
int main() {
	int n, rem, tmp, dec = 0, b=1;
    cin>>n;
    for( int i=1;n!=0;i++)
    {
        rem = n % 10;
        dec = dec + rem * b;
        b *= 2;
        n = n/ 10;
    }
    cout<<dec;
}
